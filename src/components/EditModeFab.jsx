// EditModeFab is intentionally empty — edit mode is activated automatically
// inside the admin panel's iframe viewer (/?admin=1), not via a floating button.
export function EditModeFab() {
  return null
}
